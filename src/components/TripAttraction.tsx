import * as React from 'react';

const TripAttraction: React.FC = () => {
  return (
    <div>
      ATTRACTION
      <button>Modify</button>
      <button>Delete</button>
    </div>
  );
};

export default TripAttraction;
