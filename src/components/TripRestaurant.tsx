import * as React from 'react';

const TripRestaurant: React.FC = () => {
  return (
    <div>
      RESTAURANT
      <button>Modify</button>
      <button>Delete</button>
    </div>
  );
};

export default TripRestaurant;
