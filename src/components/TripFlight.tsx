import * as React from 'react';

const TripFlight: React.FC = () => {
  return (
    <div>
      FLIGHT
      <button>Modify</button>
      <button>Delete</button>
    </div>
  );
};

export default TripFlight;
